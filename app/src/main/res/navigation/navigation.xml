<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/articlesFragment">

    <fragment
        android:id="@+id/articlesFragment"
        android:name="com.yuchen.publisher.articles.ArticlesFragment"
        android:label="fragment_articles"
        tools:layout="@layout/fragment_articles" />
    <dialog
        android:id="@+id/publishDialog"
        android:name="com.yuchen.publisher.publish.PublishDialog"
        android:label="dialog_publish"
        tools:layout="@layout/dialog_publish" />
    <action android:id="@+id/action_global_publishDialog" app:destination="@id/publishDialog" />
</navigation>